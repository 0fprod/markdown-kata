
describe('Tests de lectura', () => {
  it('es capaz de leer un fichero markdown', () => {
    // Arrange
    const ruta = './markdown-repository/ejemplo.md'
    // Act

    // Assert
  })
})
